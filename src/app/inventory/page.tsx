import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Inventory | Primeval Games",
  description: "Check out what we carry.",
};

const InventoryPage = () => {
  return (
    <div>Inventory Page</div>
  )
}

export default InventoryPage