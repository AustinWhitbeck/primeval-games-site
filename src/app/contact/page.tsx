import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Contact Us | Primeval Games",
  description: "Have a question?",
};

const ContactUsPage = () => {
  return (
    <div>Contact Us Page</div>
  )
}

export default ContactUsPage